<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/OpenSilverPdfViewer;component/Styles/BrushStyles.xaml"/>
    </ResourceDictionary.MergedDictionaries>
   
    <Style x:Key="CMSHorizontalSeparatorStyle" TargetType="Separator">
        <Setter Property="Height" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <StackPanel 
                        Height="2" >
                        <Rectangle Height="1" Fill="{StaticResource KDSSeparatorStartBrush}" />
                        <Rectangle Height="2" Fill="{StaticResource KDSSeparatorEndBrush}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CMSVerticalSeparatorStyle" TargetType="Separator">
        <Setter Property="Width" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <StackPanel 
                        Orientation="Horizontal" 
                        Width="2" >
                        <Rectangle Width="1" Fill="{StaticResource KDSSeparatorStartBrush}" />
                        <Rectangle Width="2" Fill="{StaticResource KDSSeparatorEndBrush}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
